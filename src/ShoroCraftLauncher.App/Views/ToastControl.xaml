<UserControl x:Class="ShoroCraftLauncher.App.Views.ToastControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:models="clr-namespace:ShoroCraftLauncher.App.Models"
             Width="340" Margin="0,0,0,12">
    <Border x:Name="Root" Background="#171722" BorderBrush="#2A2A3A" BorderThickness="1" CornerRadius="8"
            Padding="0" ClipToBounds="True">
        <Border.RenderTransform>
            <TranslateTransform x:Name="Tf" X="40" />
        </Border.RenderTransform>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="4" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border x:Name="Accent" Grid.Column="0" Background="#4caf50" />

            <Grid Grid.Column="1" Margin="14,12,12,12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Name="TitleText" Grid.Column="0" Text="{Binding Title}"
                               FontSize="13" FontWeight="Bold" Foreground="#FFFFFF" TextWrapping="Wrap" />
                    <Button Grid.Column="1" Content="✕" Command="{Binding DismissCommand}"
                            Background="Transparent" BorderThickness="0" Foreground="#8A8A9A"
                            FontSize="12" Width="22" Height="22" Padding="0" Margin="8,0,0,0"
                            Cursor="Hand" />
                </Grid>

                <TextBlock Grid.Row="1" Text="{Binding Message}" Margin="0,6,0,0"
                           FontSize="12" Foreground="#C8C8D4" TextWrapping="Wrap" />

                <ItemsControl Grid.Row="2" ItemsSource="{Binding Actions}" Margin="0,10,0,0"
                              HorizontalAlignment="Right">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="models:ToastAction">
                            <Button Content="{Binding Label}" Command="{Binding Command}"
                                    Style="{StaticResource SecondaryButton}" Margin="6,0,0,0"
                                    Padding="12,5" FontSize="12" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </Grid>
    </Border>
</UserControl>
