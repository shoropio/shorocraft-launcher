<UserControl x:Class="ShoroCraftLauncher.App.Views.ResourcePacksView"
             x:Name="root"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Style="{StaticResource CardBorder}" Margin="0,0,12,0" Padding="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="{DynamicResource RPacks_Profiles}" Style="{StaticResource CardTitle}" Margin="0,0,0,12" />
                <ListBox Grid.Row="1" ItemsSource="{Binding Profiles}"
                         SelectedItem="{Binding SelectedProfile}"
                         Background="Transparent" BorderThickness="0"
                         DisplayMemberPath="Name" />
            </Grid>
        </Border>

        <Grid Grid.Column="1" Margin="0,0,18,0" Background="{StaticResource BackgroundBrush}" IsEnabled="True">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <WrapPanel Grid.Row="0" Margin="0,0,0,12">
                <Button Content="{DynamicResource RPacks_Add}" Command="{Binding AddPackCommand}"
                        Style="{StaticResource PrimaryButton}" Margin="0,0,10,10" />
                <Button Content="{DynamicResource RPacks_OpenFolder}" Command="{Binding OpenFolderCommand}"
                        Style="{StaticResource SecondaryButton}" Margin="0,0,10,10" />
                <Button Content="{DynamicResource RPacks_Refresh}" Command="{Binding RefreshCommand}"
                        Style="{StaticResource SecondaryButton}" Margin="0,0,0,10" />
            </WrapPanel>

            <ProgressBar Grid.Row="1" Height="4" Margin="0,0,0,12" IsIndeterminate="True"
                         Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}"
                         Style="{StaticResource AccentProgressBar}" />

            <TextBlock Grid.Row="2" Text="{Binding StatusMessage}"
                       Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,8" />

            <ListBox Grid.Row="2" ItemsSource="{Binding Packs}"
                     Background="Transparent" BorderThickness="0">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem" BasedOn="{StaticResource FlushListBoxItem}">
                                <Setter Property="Margin" Value="0,0,18,8" />
                            </Style>
                        </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource CardBorder}" Padding="14">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="48" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Width="40" Height="40"
                                        Background="{StaticResource SurfaceBrush}"
                                        CornerRadius="0" Margin="0,0,10,0"
                                        BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                                    <TextBlock Text="🎨" FontSize="20"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center" />
                                </Border>
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="{Binding Name}" Style="{StaticResource CardTitle}" />
                                    <TextBlock Text="{Binding FileSizeBytes, Converter={StaticResource FileSizeConverter}}"
                                               Style="{StaticResource CardSubtitle}" Margin="0,4,0,0" />
                                </StackPanel>
                                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                    <ToggleButton IsChecked="{Binding Status, Converter={StaticResource StatusToBoolConverter}}"
                                                  Style="{StaticResource SwitchToggle}" />
                                    <Button Content="{DynamicResource RPacks_Delete}" Style="{StaticResource DangerButton}"
                                            Padding="10,5" FontSize="12" Margin="10,0,0,0"
                                            Command="{Binding ElementName=root, Path=DataContext.RemovePackCommand}"
                                            CommandParameter="{Binding Id}" />
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </Grid>
</UserControl>
