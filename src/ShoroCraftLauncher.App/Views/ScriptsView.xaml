<UserControl x:Class="ShoroCraftLauncher.App.Views.ScriptsView"
             x:Name="root"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Style="{StaticResource CardBorder}" Margin="0,0,12,0">
            <StackPanel>
                <TextBlock Text="Perfiles" Style="{StaticResource CardTitle}" Margin="0,0,0,12" />
                <ListBox ItemsSource="{Binding Profiles}"
                         SelectedItem="{Binding SelectedProfile}"
                         Background="Transparent" BorderThickness="0"
                         DisplayMemberPath="Name" Height="200" />

                <TextBlock Text="Scripts" Style="{StaticResource CardTitle}" Margin="0,16,0,12" />
                <Button Content="+ Importar Script" Command="{Binding ImportScriptCommand}"
                        Style="{StaticResource PrimaryButton}" Margin="0,0,0,8" />
                <ListBox ItemsSource="{Binding Scripts}"
                         SelectedItem="{Binding SelectedScript}"
                         Background="Transparent" BorderThickness="0"
                         DisplayMemberPath="Name" Height="250" />
            </StackPanel>
        </Border>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                <TextBlock Text="{Binding SelectedScript.Name, TargetNullValue='Selecciona un script'}"
                           Style="{StaticResource CardTitle}" VerticalAlignment="Center" />
            </StackPanel>

            <TextBlock Grid.Row="1" Text="{Binding StatusMessage}"
                       Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4" />

            <Border Grid.Row="2" Style="{StaticResource CardBorder}" Padding="0">
                <TextBox Text="{Binding ScriptContent, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource ConsoleTextBox}"
                         AcceptsReturn="True" TextWrapping="Wrap"
                         FontSize="13" Height="400" />
            </Border>

            <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,8,0,0">
                <Button Content="Guardar (con backup)" Command="{Binding SaveScriptCommand}"
                        Style="{StaticResource PrimaryButton}" Margin="0,0,8,0" />
                <Button Content="Eliminar" Command="{Binding DeleteScriptCommand}"
                        Style="{StaticResource DangerButton}" />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
